name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build:
    name: Build & Docker
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Restore
        run: dotnet restore VocabularyService.csproj

      - name: Build
        run: dotnet build VocabularyService.csproj -c Release --no-restore

      - uses: docker/setup-buildx-action@v3

      - name: Docker build
        run: docker build -t vocabulary-service:ci .
